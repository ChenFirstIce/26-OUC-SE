# 阿尔茨海默症筛查问卷派发、患者填写与医生统计系统

> 本文件是项目的需求基线、架构规范、Codex 执行任务书和验收清单。Codex 实施时必须先完整读取本文件和 `目标.md`，再开始创建代码。

## 1. 项目定位

项目暂定名：`AD-Questionnaire`  
中文名：阿尔茨海默症筛查问卷与数据统计系统

系统需要跑通以下完整闭环：

1. 医生登录并创建或选择患者档案。
2. 医生从已发布的量表/问卷模板中选择一个或多个任务，组成派发包。
3. 系统生成患者填写链接、二维码和 6 位访问码。
4. 患者用手机浏览器打开链接，验证访问码后填写问卷。
5. 患者填写过程自动保存，可以退出后继续填写。
6. 患者提交后，后端保存结构化答案并执行适用的规则计分。
7. 医生查看原始答案、总分、维度得分、风险提示和历史趋势。
8. 管理员查看全局数据，医生查看本人患者明细和本科室匿名汇总。
9. 系统支持筛选、下钻、CSV 导出和统计图 PNG 导出。

本项目是筛查和信息管理系统，不是诊断系统。页面和报告只能使用“筛查结果”“风险提示”“建议进一步专业评估”等表述，不得输出“已确诊阿尔茨海默症”。

## 2. 本期范围

### 2.1 必须完成

- 医生、管理员登录和权限隔离。
- 医生维护本人负责的最小患者档案。
- 元数据驱动的问卷模板、分区、题目、选项、校验和计分配置。
- 问卷模板草稿、发布版本和归档。
- 单问卷或多问卷任务包派发。
- 患者移动端填写、自动保存、恢复、提交和防重复提交。
- 通用规则计分、风险分级和需要医生人工复核的结果状态。
- 医生患者详情、问卷结果和趋势查看。
- 医生个人统计、本科室匿名聚合、管理员全局统计。
- 患者与历史评估 CSV 导入。
- CSV 明细导出、ECharts 图表 PNG 导出。
- 操作审计、匿名演示数据、自动化测试和 Windows 一键运行脚本。

### 2.2 本期不做

- 微信小程序原生版本；患者端先使用移动 Web/H5。
- LLM 对话式访谈、语音识别、图片识别和开放回答语义评分。
- MMSE、AVLT、VFT、STT、Boston 命名等需要计时、播音、轨迹或医生主持的 B 类交互任务。
- 对接医院 EHR、短信平台、微信服务通知或真实身份认证。
- 根据统计数据自动作出疾病诊断。
- 在来源模板缺失时自行编造正式医学题目、评分阈值或医学解释。

## 3. 用户和权限

### 3.1 管理员 `admin`

- 查看全院患者、派发、填写、评估和统计。
- 管理医生账号、科室、问卷模板和量表字典。
- 创建、编辑、预览、发布和归档问卷版本。
- 导入历史数据、下载导入错误报告、导出全局脱敏数据。
- 查看登录、派发、提交、导入、导出和管理操作审计记录。

### 3.2 医生 `doctor`

- 创建和维护本人负责的患者档案。
- 查看本人负责患者的明细、答案和评估结果。
- 选择已发布问卷，向本人患者派发任务。
- 撤销尚未提交的派发、延长截止时间、重新生成访问码。
- 查看个人统计和本科室匿名聚合。
- 只能导出本人负责患者的脱敏数据。
- 不允许编辑标准问卷内容、风险阈值或其他医生患者。

### 3.3 患者 `patient-link`

- 本期不创建长期患者账号。
- 患者通过不可猜测的派发令牌链接/二维码和独立 6 位访问码进入任务。
- 患者令牌只允许访问对应派发包，不能访问其他患者、医生端接口或统计接口。
- 患者只能在有效期内保存和提交答案。
- 默认只展示提交成功信息，不直接向患者展示风险等级和医学解释。

### 3.4 权限硬规则

- 所有数据范围必须由后端根据当前身份强制添加，不能相信前端传入的 `doctor_id`、`department_id` 或 `patient_id`。
- 医生患者明细条件固定为 `assigned_doctor_id == current_user.id`。
- 医生可查看本科室聚合，但任何人数小于 5 的分组显示为“<5”且不允许下钻。
- 管理员、医生和患者链接分别使用不同的权限依赖。
- 用户、模板和患者使用停用/归档，不进行物理删除。

## 4. 技术架构

### 4.1 技术栈

- 前端：Vue 3、TypeScript、Vite、Vue Router、Pinia、Element Plus、ECharts、Axios。
- 后端：Python 3.12、FastAPI、SQLAlchemy、Pydantic、Alembic。
- 认证：医生/管理员使用 JWT；患者使用派发令牌和访问码。
- 密码：PBKDF2-HMAC-SHA256 哈希；访问码和患者令牌只保存哈希或不可逆摘要。
- 数据库：默认 SQLite；所有连接通过 `DATABASE_URL`，保证后续可换 PostgreSQL。
- 后端测试：Pytest。
- 前端测试：Vitest。
- 浏览器验收：Playwright，至少覆盖 Chromium 的桌面端和移动端视口。
- 前端包管理：npm，并提交锁文件。
- 日期：数据库保存 UTC，前端显示 `Asia/Shanghai`。
- API：统一前缀 `/api/v1`，自动提供 OpenAPI/Swagger。

### 4.2 单仓库目录原则

```text
AD-Questionnaire/
├─ backend/                 # FastAPI 应用、迁移、种子和后端测试
├─ frontend/                # 医生/管理端和患者移动端 Vue 应用
├─ docs/                    # 需求、数据字典、接口、统计口径、演示说明
├─ samples/                 # CSV 和问卷 JSON 示例
├─ scripts/                 # Windows 初始化、启动、停止、测试脚本
├─ .env.example
└─ README.md
```

不得创建两个互相复制公共类型和接口逻辑的独立前端。医生/管理端和患者端使用同一个 Vue 工程中的两套布局与路由分区，共享 API、类型、校验和基础组件。

### 4.3 后端模块

每个业务模块包含 `router`、`schemas`、`service`、`repository/model` 和对应测试。路由只负责参数接收、权限依赖和响应，不能放统计公式或业务状态流转。

- `auth`：登录、JWT、当前用户、密码与权限依赖。
- `users`：医生、管理员账号。
- `departments`：科室字典。
- `patients`：患者档案和负责医生。
- `questionnaires`：模板、版本、分区、题目、选项和发布。
- `assignments`：派发包、派发项、令牌、访问码和状态机。
- `responses`：草稿、答案、提交、幂等控制。
- `scoring`：通用计分器、复杂量表计分插件、风险区间、人工复核。
- `assessments`：提交后形成的标准评估结果。
- `statistics`：统一统计口径和聚合查询。
- `imports`：CSV/JSON 模板导入、校验和错误报告。
- `exports`：按权限和筛选条件导出。
- `audit`：审计日志。
- `core`：配置、数据库、异常、响应、日志和安全工具。

`questionnaires` 不直接写统计逻辑，`statistics` 不解析题目配置，`responses` 不自行决定风险等级。一次提交的数据流固定为：

```text
Assignment -> Response submission -> Scoring service -> Assessment -> Statistics
```

## 5. 问卷引擎

### 5.1 模板和版本

- 问卷模板包含编码、名称、说明、填写对象、预计时长、分类和状态。
- 模板状态：`draft`、`published`、`archived`。
- 每次发布生成不可变版本；已派发任务始终引用发布时的版本。
- 修改已发布模板时必须创建新版本，不能改变既有患者答案的题目解释。
- 模板归档后不能新派发，但历史记录继续可见。
- 提供 JSON 导入/导出能力，便于后续增加量表而不改页面代码。

### 5.2 题型

第一版统一支持：

- 单行文本 `short_text`
- 多行文本 `long_text`
- 整数 `integer`
- 小数 `number`
- 日期 `date`
- 时间 `time`
- 时长 `duration`
- 是/否 `yes_no`
- 单选 `single_choice`
- 多选 `multi_choice`
- 有序等级 `scale`

每道题具有稳定的 `question_key`，并支持：

- 标题、填写说明、是否必填、显示顺序。
- 最小/最大值、文本长度、日期范围等校验。
- 选项值、中文标签和分值。
- 简单条件显示，例如“上一题选择是时显示病程年数”。
- 所属维度和是否属于分析字段。

条件规则使用受控 JSON，不允许在数据库保存和执行任意 Python/JavaScript 表达式。

### 5.3 计分策略

支持三类策略：

1. `metadata_sum`：根据选项分值、反向分值、维度题目集合和总分配置求和。
2. `registered_calculator`：PSQI 等复杂规则通过后端注册的版本化计算器执行，每个计算器必须有独立单元测试。
3. `manual_review`：CDR 等不能由固定患者答案直接得到正式分数的量表，只保存答案并标记“待医生复核”。

风险配置使用有序阈值列表，输出 `low`、`medium`、`high` 或 `unknown`。没有获得正式来源的量表不得配置虚构阈值，只能输出总分或“待复核”。

### 5.4 初始表单目录

按照 `目标.md` 中的 A 类患者可填写范围建立模板骨架：

- 一般资料。
- 既往史。
- 个人史。
- SCD-Q9。
- 爱丁堡利手量表。
- GDS-15。
- PSQI。
- RBDSQ。
- ESS。
- 情景严重度选择题。
- CDR 受试者版。
- CDR 知情者版。

当前文件夹没有上述正式量表题目原文和完整评分手册，因此 Codex 必须：

- 先实现完整问卷引擎和可运行的“演示问卷”。
- 演示题显著标注“非临床演示内容”。
- 为每个目标量表创建可导入的 JSON 模板文件骨架。
- 不得凭常识编造正式题目或评分阈值。
- 后续拿到授权量表后，只替换/导入模板数据和对应计分配置。

## 6. 派发与填写流程

### 6.1 医生派发

1. 医生选择本人患者。
2. 选择一个或多个已发布问卷版本。
3. 设置任务名称、截止时间和备注。
4. 后端创建一个派发包和多个派发项。
5. 系统生成填写链接、二维码和独立 6 位访问码。
6. 页面支持复制链接、打印二维码、重新生成访问码。

派发包状态：

- `pending`：未打开。
- `in_progress`：至少一个问卷已开始。
- `submitted`：所有必做问卷已提交。
- `reviewed`：医生已查看并确认。
- `expired`：已过截止时间。
- `revoked`：医生撤销。

单个派发项独立记录 `not_started`、`draft`、`submitted`、`reviewed`，避免多问卷任务必须一次完成。

### 6.2 患者填写

- 患者路由：`/p/fill/:token`。
- 第一次进入输入 6 位访问码，验证成功后建立短期患者填写会话。
- 展示任务说明、问卷列表、完成进度和预计用时。
- 每页/每分区填写，支持上一步、下一步。
- 答案变更后 5 秒防抖自动保存；切换页面时再次保存。
- 网络失败时保留本地暂存并提示重试，恢复网络后同步。
- 提交前执行前端校验，后端再次执行同一版本的权威校验。
- 提交接口带幂等键；重复点击不能产生两条评估。
- 提交后锁定患者修改权限，只显示成功页和联系医生提示。
- 已过期或撤销任务可以查看明确状态，但不能继续填写。

### 6.3 医生复核

- 医生可查看每题原始答案、题目版本、提交时间和填写耗时。
- 自动计分量表展示总分、维度分、风险等级和统计口径说明。
- 人工复核量表允许医生填写候选分数、复核备注和确认状态。
- 医生确认结果后写入审计日志；历史答案和自动分数不可被覆盖。

## 7. 核心数据模型

- `users`：账号、密码哈希、姓名、角色、科室、状态。
- `departments`：科室编码、名称、状态。
- `patients`：外部患者编码、最小人口信息、所属科室、负责医生、状态。
- `questionnaire_templates`：模板基本信息和状态。
- `questionnaire_versions`：模板版本、计分策略、风险配置、发布时间。
- `questionnaire_sections`：版本内分区。
- `questions`：版本内题目、题型、校验、条件和维度。
- `question_options`：选项值、标签、顺序和分值。
- `assignment_packages`：患者、医生、截止时间、令牌摘要、访问码摘要、状态。
- `assignment_items`：派发包内的问卷版本和状态。
- `responses`：派发项、草稿/提交状态、开始/提交时间、幂等键。
- `response_answers`：题目键、结构化答案 JSON、题目得分。
- `assessments`：总分、维度分 JSON、风险、来源、复核状态。
- `import_jobs`：导入批次及错误报告。
- `audit_logs`：操作人、操作、对象、结果、时间和请求标识。

数据库为所有外键、唯一编码、派发令牌、幂等键和常用统计条件建立索引。不得把整份问卷答案只存成一个无法查询的大 JSON；答案使用逐题记录，维度汇总可使用 JSON。

## 8. 前端页面

### 8.1 医生/管理端

- `/login`：登录。
- `/dashboard`：角色对应综合看板。
- `/patients`：患者列表、创建、筛选。
- `/patients/:id`：患者档案、派发记录、评估时间线。
- `/assignments`：派发列表、状态和完成进度。
- `/assignments/create`：选择患者和问卷并派发。
- `/assignments/:id`：二维码、访问码、问卷进度和答案结果。
- `/statistics`：人口结构、派发完成、量表结果、风险、工作量、数据质量。
- `/admin/questionnaires`：模板列表、编辑、预览、发布、归档。
- `/admin/users`：医生账号。
- `/admin/departments`：科室。
- `/admin/imports`：历史数据导入。
- `/admin/audit`：审计日志。

### 8.2 患者移动端

- 访问码验证页。
- 知情提示与任务说明页。
- 问卷任务列表和进度页。
- 分区式填写页。
- 提交确认页。
- 提交成功/已过期/已撤销页。

患者端设计要求：

- 以 375px 手机宽度为基础设计。
- 点击区域不小于 44×44px。
- 正文字号不小于 16px。
- 单选和多选整行可点击。
- 当前进度、必填提示、保存状态清晰可见。
- 不能只依赖颜色传达错误或风险。

## 9. 统计指标

### 9.1 综合指标

- 患者总数。
- 已派发患者数。
- 派发包数量。
- 问卷提交数量。
- 派发完成率：完成全部必做项的派发包数 ÷ 有效派发包数。
- 平均完成时长：开始到提交的中位数和平均值。
- 高风险患者数与高风险率。
- 待医生复核数量。

### 9.2 派发漏斗

按 `已派发 -> 已打开 -> 填写中 -> 已提交 -> 已复核` 展示数量和转化率，同时统计过期和撤销数量。

### 9.3 量表与风险

- 各问卷派发量、提交量、完成率和平均用时。
- 各量表总分与维度分分布。
- 风险等级分布。
- 患者同一量表的历次得分趋势。
- 高风险患者口径：每名患者每种量表取筛选时间内最新有效评估；任一量表为高风险则计入。

### 9.4 管理统计

- 医生和科室的患者数、派发数、完成数、完成率、待复核数。
- 年龄段、性别、教育程度和居住类型结构。
- 数据完整度、未知风险数量、无效导入行和重复记录。

### 9.5 筛选和导出

- 默认最近 12 个月。
- 支持日期、科室、医生、问卷、风险、派发状态、性别和年龄段筛选。
- 医生端隐藏无权限筛选项；服务端仍须校验。
- 图表可导出当前筛选结果 PNG。
- 表格可导出当前权限和筛选条件下的 UTF-8 BOM CSV。

## 10. API 契约

统一响应错误结构：

```json
{
  "code": "ASSIGNMENT_EXPIRED",
  "message": "该任务已过期",
  "request_id": "..."
}
```

分页结构：

```json
{
  "items": [],
  "total": 0,
  "page": 1,
  "page_size": 20
}
```

主要接口：

```text
POST   /api/v1/auth/login
GET    /api/v1/auth/me

GET    /api/v1/patients
POST   /api/v1/patients
GET    /api/v1/patients/{id}
PATCH  /api/v1/patients/{id}
GET    /api/v1/patients/{id}/timeline

GET    /api/v1/questionnaires
POST   /api/v1/questionnaires
GET    /api/v1/questionnaires/{id}
PATCH  /api/v1/questionnaires/{id}
POST   /api/v1/questionnaires/{id}/publish
POST   /api/v1/questionnaires/{id}/archive
POST   /api/v1/questionnaires/import
GET    /api/v1/questionnaires/{id}/export

GET    /api/v1/assignments
POST   /api/v1/assignments
GET    /api/v1/assignments/{id}
POST   /api/v1/assignments/{id}/revoke
POST   /api/v1/assignments/{id}/extend
POST   /api/v1/assignments/{id}/regenerate-code
POST   /api/v1/assignments/{id}/review

POST   /api/v1/patient-session/verify
GET    /api/v1/patient-session/tasks
GET    /api/v1/patient-session/tasks/{item_id}
PUT    /api/v1/patient-session/tasks/{item_id}/draft
POST   /api/v1/patient-session/tasks/{item_id}/submit

GET    /api/v1/statistics/overview
GET    /api/v1/statistics/funnel
GET    /api/v1/statistics/demographics
GET    /api/v1/statistics/trends
GET    /api/v1/statistics/risks
GET    /api/v1/statistics/questionnaires
GET    /api/v1/statistics/workload
GET    /api/v1/statistics/data-quality

POST   /api/v1/imports/patients
POST   /api/v1/imports/assessments
GET    /api/v1/imports
GET    /api/v1/imports/{id}/errors
GET    /api/v1/exports/assessments.csv
```

## 11. 演示数据和导入

Codex 必须生成确定性匿名演示数据：

- 3 个科室。
- 1 个管理员、至少 6 个医生。
- 至少 100 名匿名患者。
- 至少 300 个派发项。
- 包含未打开、填写中、已提交、已复核、过期和撤销状态。
- 包含不同风险等级、年龄段和历史趋势。

患者 CSV：

```text
patient_code,sex,birth_date,education_level,residence_type,department_code,doctor_username
```

历史评估 CSV：

```text
assessment_code,patient_code,questionnaire_code,questionnaire_version,assessed_at,total_score,risk_level,dimension_scores_json
```

规则：

- 模板文件使用 UTF-8 BOM，导入兼容 UTF-8、UTF-8 BOM、GB18030。
- 患者按 `patient_code` 幂等更新非空字段。
- 评估按 `assessment_code` 唯一；重复跳过并写入错误报告。
- 未知医生、科室、问卷、非法日期、非法 JSON 和非法风险等级逐行报错。
- 允许部分成功并生成带行号、字段和原因的错误 CSV。

## 12. 安全、隐私与审计

- 演示环境禁止录入真实患者信息。
- 不采集身份证、电话、详细地址等非必要字段。
- 密码、访问码、令牌和密钥不得写入日志。
- `.env.example` 只包含占位值，真实 `.env` 不提交。
- 登录、派发、提交、复核、导入、导出、账号变更和模板发布写入审计。
- CORS 只允许配置中的前端地址。
- 所有输入由 Pydantic 和业务规则双重校验。
- 患者端接口限流；访问码连续失败需要短时锁定。
- CSV 导出默认使用患者编码，不导出姓名等直接身份信息。

## 13. 测试与验收

### 13.1 后端测试

- 登录成功、密码错误、停用账号、过期 JWT。
- 医生不能读取、修改或派发给其他医生患者。
- 医生不能通过请求参数扩大统计或导出范围。
- 患者令牌只能访问对应派发包。
- 访问码错误、过期、撤销和重新生成后的旧码失效。
- 模板发布后不可变；新版本不影响历史派发。
- 所有题型保存、恢复和提交。
- 条件题、必填、数值范围和非法答案校验。
- 自动保存的版本冲突处理。
- 提交幂等，重复请求只生成一个评估。
- 通用计分、维度分、反向分和风险阈值。
- 人工复核量表不产生虚构自动分数。
- 派发漏斗、高风险、完成率和趋势统计公式。
- CSV 正常、重复、部分失败和错误报告。

### 13.2 前端与端到端测试

- 管理员登录、发布演示问卷、查看全局统计。
- 医生建患者、派发两个问卷并看到二维码和访问码。
- 手机视口进入患者链接、验证、填写一半、刷新后恢复。
- 患者完成两个问卷并提交。
- 医生端状态实时更新，查看答案、得分并完成复核。
- 统计看板数量随提交发生可验证变化。
- 医生只看到本人明细和本科室匿名聚合。
- CSV 和 PNG 能真实下载并打开。
- 空数据、接口失败、无权限、任务过期和网络恢复状态可见。

### 13.3 完成定义

以下条件全部满足才允许宣布完成：

- 新环境按 README 能成功初始化和启动。
- 前端、后端、数据库真实联通，不使用仅前端 Mock 接口冒充后端。
- 数据库迁移、种子脚本可重复执行。
- 医生派发到患者提交再到统计更新的完整浏览器流程跑通。
- 自动保存、继续填写、计分、复核和权限隔离均有自动化测试。
- 前端生产构建、后端测试、前端测试、E2E 测试全部通过。
- Swagger 可访问，示例请求可成功调用。
- Codex 最终报告列出实际执行的命令、通过项、失败项和未验证项。
- 不能把静态代码检查、数据库结构检查或模拟结果表述为真实浏览器验收。

## 14. Codex 执行顺序

Codex 不得一次性堆完所有文件后再测试，应按以下阶段逐步实现并验证：

1. 创建单仓库、环境配置、迁移、统一异常和健康检查。
2. 完成用户、科室、患者和登录权限，先通过权限测试。
3. 完成元数据问卷引擎、模板版本与演示问卷，通过题型和发布测试。
4. 完成派发状态机、患者令牌和访问码，通过越权与过期测试。
5. 完成患者移动端、草稿自动保存、恢复和幂等提交。
6. 完成通用计分、人工复核和评估结果。
7. 完成医生患者详情、派发管理和结果页面。
8. 完成统计服务和管理员/医生看板。
9. 完成 CSV/JSON 导入导出、图表 PNG 和审计。
10. 生成演示数据，执行完整 E2E 闭环。
11. 编写 README、数据字典、统计口径、问卷模板格式和演示脚本。
12. 从干净数据库重新执行安装、迁移、种子、启动、测试和浏览器验收。

Windows 主运行入口：

```powershell
.\scripts\setup.ps1
.\scripts\start.ps1
```

同时提供：

```powershell
.\scripts\test.ps1
.\scripts\stop.ps1
```

启动脚本必须检查 Python、Node、端口、`.env` 和数据库状态，最后明确输出医生端地址、患者演示链接、Swagger 地址和演示账号。停止脚本只能结束本项目记录的进程。

## 15. Codex 实施约束

- 先实现最小纵向闭环，再扩展页面和统计，不能先只画静态看板。
- 不得硬编码医生、患者、科室或问卷 ID。
- 不得在 Vue 页面复制计分和统计公式。
- 不得使用任意 `eval` 执行数据库内规则。
- 不得伪造正式医学量表题目、阈值和解释。
- 所有新增量表优先通过 JSON 模板和受控计分配置完成。
- 保持每个模块职责单一，公共组件只承载真正复用的行为。
- 所有列表都使用服务端分页、筛选和稳定排序。
- 每完成一个阶段就运行对应测试；遇到失败先定位并修复，不跳过测试。
- 项目交付必须是真实可运行系统，不接受只有 UI、只有接口或只有设计文档。
